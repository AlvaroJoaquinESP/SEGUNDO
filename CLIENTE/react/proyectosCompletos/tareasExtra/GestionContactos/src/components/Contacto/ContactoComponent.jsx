import { PropTypes } from "prop-types";

const Contacto = ({obj}) => {

  return (
    <div>
        <h1>{obj.id}</h1>
        <h1>{obj.id}</h1>
        <h1>{obj.id}</h1>
        <h1>{obj.id}</h1>


    </div>
  )
}

Contacto.propTypes = {
    obj : PropTypes.object
}

export default Contacto

